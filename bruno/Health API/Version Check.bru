meta {
  name: Version Check
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/health/version
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should have version info", function() {
    expect(res.getBody().data).to.have.property('version');
    expect(res.getBody().data).to.have.property('apiVersion');
  });
}
