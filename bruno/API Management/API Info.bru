meta {
  name: API Info
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/api
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should have endpoints array", function() {
    expect(res.getBody().data).to.have.property('endpoints');
    expect(res.getBody().data.endpoints).to.be.an('array');
  });
}
