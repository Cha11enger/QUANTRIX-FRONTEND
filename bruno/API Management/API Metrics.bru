meta {
  name: API Metrics
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/api/metrics
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should have metrics array", function() {
    expect(res.getBody().data).to.have.property('metrics');
    expect(res.getBody().data.metrics).to.be.an('array');
  });
}
