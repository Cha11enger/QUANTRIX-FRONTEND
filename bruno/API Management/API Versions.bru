meta {
  name: API Versions
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/api/versions
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should have versions array", function() {
    expect(res.getBody().data).to.have.property('versions');
    expect(res.getBody().data.versions).to.be.an('array');
  });
}
